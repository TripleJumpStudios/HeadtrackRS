[package]
name = "headtrack-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pose types, filter math, and pipeline traits for headtrack-rs"

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
